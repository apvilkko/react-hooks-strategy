# Strategy Pattern with React Hooks

This repository demonstrates how the Strategy Pattern can be implemented when your application logic uses React hooks.

To run, clone this repo and install dependencies:

```
yarn
```

Then start the app:

```
yarn start
```
